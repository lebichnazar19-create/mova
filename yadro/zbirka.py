"""Відомості про збірку — для першого рядка «Довідки» в застосунку.
У репозиторії лежать типові значення (локальна збірка); workflow перед
збіркою APK перезаписує цей файл датою (UTC) і коротким хешем коміту
(файл у .gitignore не потрібен — CI змінює лише свою копію)."""

ДАТА = "локальна"
КОМІТ = "—"
