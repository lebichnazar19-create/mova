# Звіт: методи списків, від'ємні індекси, зрізи

## Що додано

| Можливість | Синтаксис/виклик | Поведінка |
|---|---|---|
| `додай(список, х)` | `додай(а, 4)` | дописує `х` у кінець списку **на місці**, повертає `ніщо` |
| `вилучи(список, і)` | `хай х = вилучи(а, 0)` | вилучає елемент за індексом (від'ємний теж) **на місці**, повертає вилучений елемент |
| `сортуй(список)` | `сортуй(а)` | сортує список за зростанням **на місці**, повертає `ніщо`; лише однорідний список (усі числа або усі рядки) — інакше помилка виконання |
| Від'ємний індекс | `а[-1]`, `а[-1] = 9`, `с[-1]` | рахується з кінця (як у Python); поза межами навіть після приведення — помилка виконання |
| Зріз | `а[1:3]`, `а[:2]`, `а[1:]`, `а[:]`, від'ємні межі теж — для списків і рядків, лише читання | межі лагідно обрізаються до `[0, довжина]`, ніколи не падає через вихід за межі; кроку (`[::2]`) і присвоєння в зріз немає |

Реалізовано як мовні («МОВНІ», прямо в `ядро/вм.py`) вбудовані функції —
той самий рівень, що й `довжина`/`діапазон`, без нового синтаксису
виклику. Зрізи — новий синтаксис (`[a:b]`), новий вузол AST `Зріз`, новий
опкод `SLICE_GET`.

## Змінені файли

- `ядро/аст.py` — вузол `Зріз(колекція, початок, кінець)`.
- `ядро/парсер.py` — `_постфікс()` розпізнає `[a:b]`/`[:b]`/`[a:]`/`[:]`
  через уже наявний токен `ДВОКРАПКА` (раніше ніде не використовувався).
- `ядро/компілятор.py` — `_в_Зріз` емітить `SLICE_GET`; відсутня межа —
  `PUSH_CONST` з константою `ніщо`, яку VM тлумачить як «з початку»/«до
  кінця».
- `ядро/вм.py`:
  - `_норм_індекс` — спільне приведення від'ємного індексу для читання й
    запису за індексом (`INDEX_GET`/`INDEX_SET`) і для `вилучи`.
  - `_зріз_get` — Python-подібна лагідна обрізка меж (на відміну від
    звичайного індексу, який падає з помилкою виконання).
  - `_додай`/`_вилучи`/`_сортуй` — нові нативні функції, зареєстровані в
    `self._native` поряд з `довжина`/`діапазон`.
- `ядро/вбудовані.py` — `додай`/`вилучи`/`сортуй` додано до `МОВНІ`
  (щоб компілятор емітив `CALL_NATIVE`, а не забороняв власну дію з таким
  іменем — див. розділ 10 `ДОВІДНИК.md`).
- `ДОВІДНИК.md` — оновлено розділи 4 (оператори), 8 (списки), 9 (рядки),
  11 (вбудовані функції), додаток (опкоди). Прибрано застарілі фрази
  «немає від'ємних індексів»/«немає зрізів»/«немає методів списку».
- `тести/test_типи.py` — 13 нових тестів (додано нижче).

## Тести

Додано в `тести/test_типи.py`, за наявним у файлі стилем (`запустити()` +
`capsys`, або пряма перевірка `ПомилкаВиконання` для помилкових шляхів):

- `test_від_ємний_індекс_список_і_рядок`, `test_від_ємний_індекс_запис`,
  `test_від_ємний_індекс_поза_межами_помилка`
- `test_зріз_списку`, `test_зріз_за_межами_не_падає_а_обрізається`,
  `test_зріз_рядка`
- `test_додай_дописує_в_кінець`
- `test_вилучи_за_індексом_повертає_елемент`,
  `test_вилучи_за_від_ємним_індексом`, `test_вилучи_поза_межами_помилка`
- `test_сортуй_числа`, `test_сортуй_рядки`,
  `test_сортуй_мішанину_типів_помилка`

Прогін повного набору тестів (без двох, що керують реальним терміналом —
`test_живий.py`, `test_екран.py`, не пов'язані зі зміною):

```
185 passed in 1.74s
```

(було 172 до змін — усі старі тести й далі проходять, додано 13 нових).

## Ручна перевірка (поза pytest)

```
мова.py --без-пристрою:
  а[-1], а[-2]              → 5 4
  а[1:3] / а[:2] / а[3:]    → [2, 3] / [1, 2] / [4, 5]
  а[:] / а[-3:-1]           → [1, 2, 3, 4, 5] / [3, 4]
  додай(а, 6)               → [1, 2, 3, 4, 5, 6]
  вилучи(а, 0) → 1, а       → [2, 3, 4, 5, 6]
  вилучи(а, -1) → 6, а      → [2, 3, 4, 5]
  сортуй([5,3,1,4,2])       → [1, 2, 3, 4, 5]
  сортуй(["вишня","яблуко","банан"]) → [банан, вишня, яблуко]
  "привіт"[-1], "привіт"[1:4] → т рив
```

Помилкові шляхи (усі дають акуратну «Помилка виконання» з рядком/`^`, а
не Python traceback):
- `вилучи(а, 5)` при довжині 3 → «Індекс 5 поза межами (довжина 3).»
- `сортуй([1, "два", 3])` → «...лише до однорідного списку...»
- `а[-10]` при довжині 3 → «Індекс -10 поза межами (довжина 3).»

## Свідомо залишено поза межами (не просили)

- Кроку в зрізі (`а[::2]`) немає.
- Присвоєння в зріз (`а[1:3] = [...]`) немає — парсер відмовляє так само,
  як і раніше для будь-якого нецілого лівого боку `=`.
- `вилучи` шукає за **індексом**, не за значенням (не `list.remove`).
- `сортуй` — лише за зростанням, без ключа сортування чи прапорця
  «спадання».
