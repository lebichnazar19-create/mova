# Протокол зв'язку телефона з платою (етап 1: пульт)

Бінарний, по серіалу (UART 115200 8N1 через USB) або по TCP (порт 5555,
ESP32 у режимі точки доступу). Телефон — ведучий: надсилає запит і чекає
відповідь; плата ніколи не пише сама.

## Запит — 4 байти

```
[команда][пін][значення_hi][значення_lo]
```

`значення` — 16 біт, старший байт перший (big-endian).

| Команда | Код | Пін | Значення | Що робить плата |
|---|---|---|---|---|
| ПІН | 1 | номер | 0 — вихід, 1 — вхід, 2 — вхід із підтяжкою | `pinMode` |
| ПИШИ | 2 | номер | 0 або 1 | `digitalWrite` |
| ЧИТАЙ | 3 | номер | — | `digitalRead` → значення відповіді |
| АНАЛОГ | 4 | номер | — | `analogRead` → значення відповіді (0..1023 Arduino, 0..4095 ESP32) |
| ШИМ | 5 | номер | 0..255 | `analogWrite` / `ledcWrite` |
| ЗАЧЕКАЙ | 6 | 0 | мс, 0..65535 | `delay(мс)`, відповідь **після** паузи |
| ПІНГ | 7 | 0 | — | відповідь: версія протоколу (зараз 1) |
| БЛИМАЙ | 8 | 0 | кількість | блимнути вбудованим світлодіодом N разів (по 200 мс), відповідь після |

## Відповідь — 3 байти

```
[статус][значення_hi][значення_lo]
```

| Статус | Значення |
|---|---|
| 0 | гаразд; `значення` — результат (ЧИТАЙ, АНАЛОГ, ПІНГ) або 0 |
| 1 | невідома команда |
| 2 | немає такого піна |
| 3 | значення поза межами |

Плата відповідає на **кожен** запит, у тому числі на ЗАЧЕКАЙ і БЛИМАЙ —
лише після того, як виконала їх. Телефон чекає відповідь до 2 с (для
ЗАЧЕКАЙ/БЛИМАЙ — 2 с плюс тривалість). Немає відповіді — «Плата не
відповідає»; агент, що повернув іншу версію на ПІНГ, відхиляється при
підключенні.

## Приклад обміну

```
-> 01 02 00 00     пін(2, вихід)
<- 00 00 00
-> 02 02 00 01     пиши(2, 1)
<- 00 00 00
-> 06 00 01 F4     зачекай(500)
<- 00 00 00        (через 500 мс)
-> 03 07 00 00     читай(7)
<- 00 00 01        значення 1
-> 07 00 00 00     пінг
<- 00 00 01        версія протоколу 1
```

Реалізації: `yadro/plata.py` (телефон: запакувати/розпакувати, транспорти
UsbSerial / WifiTcp / Симуляція), `agent/agent_arduino/agent_arduino.ino`
(лише UART), `agent/agent_esp32/agent_esp32.ino` (UART + Wi-Fi AP + TCP).
