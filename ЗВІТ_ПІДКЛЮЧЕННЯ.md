# Звіт: підключення інших `.мова` файлів (`підключи`)

## Що додано

Нове ключове слово **`підключи`** (модулі/файли): `підключи "файл.мова"`
на верхньому рівні файлу зливає дії (і сталі/змінні верхнього рівня)
підключеного файлу в поточний, так, ніби їх написано прямо тут.

```
# фігури.мова
дія площа_квадрата(сторона)
    поверни сторона * сторона
```

```
# головний.мова
підключи "фігури.мова"
друкуй(площа_квадрата(5))     # → 25
```

| Можливість | Поведінка |
|---|---|
| `підключи "шлях.мова"` | шлях — відносно директорії файлу, що підключає (не поточної робочої директорії) |
| Лише верхній рівень | всередині `дія`/`якщо`/`поки`/`для` — помилка розбору |
| Діамантове підключення | той самий файл, підключений з кількох місць, підключається лише один раз (не помилка, не виконується вдруге) |
| Файл не знайдено | `ПомилкаПідключення` з рядком/позицією/стрілкою на самій інструкції `підключи` |
| Циклічне підключення | виявляється (включно з файлом, що підключає сам себе), з ланцюжком у повідомленні (`а.мова -> б.мова -> а.мова`) |
| Конфлікт імен | та сама дія визначена і в головному, і в підключеному файлі (або в двох підключених) — явна помилка з іменами обох файлів |

Реалізовано як окремий крок **до компіляції**, а не новий опкод:
`ядро/модулі.py` рекурсивно читає й розпарсює файли й підставляє їх
інструкції на місце `підключи`, віддаючи компілятору одну плоску
програму — компілятор і VM про файли нічого не знають. Тому підтримується
лише при запуску/компіляції файлу (`мова файл.мова`, `-к`, `-д`), не в
REPL.

## Змінені файли

- `ядро/ключові.py` — нове ключове слово `"підключи": "ПІДКЛЮЧИ"`.
- `ядро/аст.py` — вузол `Підключи(шлях, рядок, позиція)`.
- `ядро/парсер.py` — розпізнає `підключи "шлях"` як інструкцію верхнього
  рівня; лічильник глибини блоку (`_глибина_блоку`) забороняє `підключи`
  всередині `дія`/`якщо`/`поки`/`для` — помилка розбору одразу при парсингу,
  а не пізніше.
- `ядро/помилки.py` — новий клас `ПомилкаПідключення` (свій заголовок
  «Помилка підключення», той самий формат рядок/позиція/`^`, що й в інших
  помилок мови).
- `ядро/модулі.py` (**новий файл**) — увесь механізм розв'язання
  підключень: рекурсивне читання/парсинг файлів відносно директорії
  того, хто підключає; кеш уже підключених файлів (діамантове
  підключення — не помилка й не подвійне виконання); стек «у процесі
  резолвиться» для виявлення циклів; перевірка конфлікту імен дій між
  різними файлами; проставляє на кожному вузлі AST з кожного файлу текст
  його власного рядка джерела (`_рядок_джерела_текст`) — щоб помилки
  компіляції в підключеному файлі показували текст саме з нього.
- `ядро/компілятор.py`:
  - `_помилка` читає `_рядок_джерела_текст` вузла (якщо є) замість завжди
    порожнього тексту — виправляє показ помилок компіляції для коду з
    підключених файлів.
  - Позиції інструкцій (`self.позиції`) тепер трійка `[рядок, кол, текст]`
    замість пари — той самий текст доступний і для помилок **виконання**
    (VM), не лише компіляції.
  - `_і_Підключи` — явна, зрозуміла помилка компіляції, якщо вузол
    `Підключи` все ж дійшов до компілятора нерозгорнутим (може статись
    лише в REPL/при прямій компіляції AST, минаючи `ядро/модулі.py`).
- `ядро/вм.py` — `_поточна_позиція`/`_помилка`/`_цикл_виконання` оновлені
  під трійку `[рядок, кол, текст]`; зворотно сумісні зі старим форматом
  пари (напр. .байт-файл, скомпільований до цієї зміни).
- `мова.py` — `_скомпілювати_або_вихід` тепер викликає
  `ядро.модулі.завантажити_програму(шлях)` замість прямого
  `парсити_текст`; ловить `ПомилкаПідключення` поряд з
  `ПомилкаРозбору`/`ПомилкаКомпіляції`. Побічний ефект: тепер і **головний**
  файл, якого не існує, дає акуратну «Помилка підключення», а не сирий
  Python-traceback (раніше `_прочитати` падало без обробки).
- `ДОВІДНИК.md` — новий розділ 11 «Підключення інших файлів (`підключи`)»
  (після розділу 10 «Дії»); старі 11-12 стали 12-13; додано рядок у вступі
  (`ядро/модулі.py`) і примітку в додатку про опкоди (`підключи` — без
  нового опкоду).
- `тести/test_підключення.py` (**новий файл**) — 15 тестів (нижче).
- `приклади/підключення/` (**нове**) — `фігури.мова` (модуль: `площа_квадрата`,
  `периметр_квадрата`, `площа_кола`) і `головний.мова` (підключає й
  використовує).

## Тести

`тести/test_підключення.py`, у стилі наявних тестів (частина — через
`ядро.модулі.завантажити_програму` напряму, частина — через CLI
`мова.команда_виконати` з `tmp_path`, за прикладом `test_помилки.py`):

- Щасливий шлях: `test_підключи_дію_з_іншого_файлу`,
  `test_підключи_кілька_дій_і_свій_код_після`,
  `test_підключи_відносно_директорії_файлу_а_не_робочої` (з `monkeypatch.chdir`
  на іншу директорію — доводить, що шлях рахується від файлу, а не cwd),
  `test_підключи_піддиректорію`,
  `test_підключи_діамантове_не_помилка_і_не_виконується_двічі`.
- Файл не знайдено: `test_підключи_файл_не_знайдено`,
  `test_підключи_файл_не_знайдено_через_cli`.
- Циклічне підключення: `test_підключи_циклічне_два_файли`,
  `test_підключи_циклічне_сам_на_себе`,
  `test_підключи_циклічне_довший_ланцюжок` (3 файли по колу).
- Конфлікт імен: `test_підключи_конфлікт_імен_дій`,
  `test_підключи_конфлікт_імен_між_двома_підключеними`,
  `test_підключи_той_самий_файл_двічі_не_конфлікт` (діамантове
  підключення не повинно хибно спрацьовувати як конфлікт саме з собою).
- Заборона поза верхнім рівнем: `test_підключи_всередині_дії_помилка_розбору`,
  `test_підключи_всередині_якщо_помилка_розбору`.

Прогін повного набору тестів (ті самі 4 файли виключено, що й раніше —
керують реальним терміналом/термукс-вібрацією, не пов'язані зі зміною):

```
191 passed in 1.93s
```

(було 176 до зміни — усі старі тести й далі проходять, додано 15 нових.)

## Ручна перевірка (поза pytest)

```
мова.py --без-пристрою приклади/підключення/головний.мова:
  Площа квадрата зі стороною 5 = 25
  Периметр квадрата зі стороною 5 = 20
  Площа кола радіусом 3 = 28.274309999999996
```

`-к`/`-б`/`-д` теж перевірено на цьому прикладі: компіляція в `.байт`,
виконання скомпільованого `.байт` окремо (без вихідних `.мова` файлів —
усе вже злито при компіляції), дизасемблювання — усі три працюють.

Помилкові шляхи (`--без-пристрою`, окремі тимчасові файли):

```
підключи "не_існує.мова":
  Помилка підключення в рядку 1, позиція 1:
      підключи "не_існує.мова"
      ^
  Файл «не_існує.мова» не знайдено (шукав: «/повний/шлях/.../не_існує.мова»).

а.мова підключає б.мова, б.мова підключає а.мова:
  Помилка підключення в рядку 1, позиція 1:
      підключи "a.мова"
      ^
  Циклічне підключення файлів: a.мова -> b.мова -> a.мова.

обидва файли визначають дію "двічі":
  Помилка підключення в рядку 2, позиція 1:
      дія двічі(х)
      ^
  Конфлікт імен: дія «двічі» вже визначена у файлі «утиліти2.мова», а
  тепер визначається ще й у «конфлікт.мова». Перейменуйте одну з них.
```

## Свідомо залишено поза межами (не просили)

- Підключення за іменем пакета/бібліотеки — лише за шляхом до файлу.
- Вибіркове підключення окремих імен (`з X підключи лише Y`) чи
  псевдоніми (`підключи X як Y`) — підключення завжди «все й під своїми
  іменами».
- Підключення в REPL (немає доступу до файлової системи в тому самому
  сенсі — рядки REPL компілюються без проходження через `ядро/модулі.py`).
- Конфлікт імен перевіряється лише для **дій** (`дія`), не для
  `хай`/`стала` верхнього рівня — вони й так живуть в окремому просторі
  імен від дій у VM (`self.глобальні` проти `self.функції`), тож
  однойменні змінна й дія одна одній не заважають; повторне оголошення
  тієї самої змінної в різних файлах і без підключення не було помилкою
  (звичайне перезаписування), і ця зміна цього не чіпає.
