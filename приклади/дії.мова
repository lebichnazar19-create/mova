# Приклад етапу 4: дія, поверни, рекурсія, локальні змінні.

дія квадрат_числа(x)
    поверни x * x

друкуй(квадрат_числа(5))

дія факторіал(n)
    якщо n <= 1
        поверни 1
    поверни n * факторіал(n - 1)

друкуй(факторіал(10))
