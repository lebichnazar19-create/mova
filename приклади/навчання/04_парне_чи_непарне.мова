# Урок 4: якщо/інакше і остача від ділення (%).
# Число парне, якщо остача від ділення на 2 дорівнює 0.

хай число = 17

якщо число % 2 == 0
    друкуй(число, "— парне")
інакше
    друкуй(число, "— непарне")
