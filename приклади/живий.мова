# Живий режим: малюємо і тримаємо картинку на екрані, поки не натиснеш «q»
# (або Ctrl+C). Якщо поверне телефон і термінал зміниться — перемалюється.

екран(20, 10)
запов_квадрат(2, 2, 6, "синій")
запов_коло(14, 5, 3, "червоний")

тримай()
