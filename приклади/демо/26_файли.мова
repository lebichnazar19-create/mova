# Файли: шляхи відносно теки програми (у застосунку — теки застосунку)
хай імя = "26_нотатки.txt"
запиши_файл(імя, "перший рядок\n")
допиши_файл(імя, "другий рядок\n")
якщо є_файл(імя)
    друкуй(прочитай_файл(імя))

спробуй
    прочитай_файл("такого_немає.txt")
якщо помилка
    друкуй("Перехопили:", помилка)
