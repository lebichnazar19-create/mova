# Рекурсія: факторіал і Фібоначчі
дія факторіал(n)
    якщо n <= 1
        поверни 1
    поверни n * факторіал(n - 1)

друкуй(факторіал(10))

дія фібо(n)
    якщо n < 2
        поверни n
    поверни фібо(n - 1) + фібо(n - 2)

для i у діапазон(0, 10)
    друкуй(фібо(i))
