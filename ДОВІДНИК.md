# Довідник мови «Мова»

Повний перелік підтримуваних ключових слів і конструкцій, з коротким
прикладом до кожної. Базується на прочитаному коді: `yadro/keywords.py`,
`yadro/lexer.py`, `yadro/parser.py`, `yadro/syntax.py`, `yadro/kompilyator.py`,
`yadro/vm.py`, `yadro/natives.py`, `yadro/errors.py`, `yadro/moduli.py`.

Для докладнішого покрокового навчання є `ПІДРУЧНИК.md`. Цей файл —
компактна довідкова таблиця для швидкого пошуку.

---

## 1. Загальний синтаксис

| Конструкція | Приклад |
|---|---|
| Блоки через відступ (4 пробіли, як у Python; без `:` і `;`) | `якщо х > 0\n    друкуй(х)` |
| Коментар до кінця рядка | `# це коментар` |
| Дужки `()`/`[]` можна переносити на кілька рядків — відступ там не рахується | `хай x = (1 +\n    2)` |

---

## 2. Літерали

| Тип | Приклад | Примітка |
|---|---|---|
| Ціле число | `42` | Python `int` |
| Дробове число | `3.14` | Python `float`; крапка обов'язково між цифрами |
| Рядок | `"текст"` або `'текст'` | однакові за змістом |
| Екрановані символи в рядку | `"рядок1\nрядок2\tтаб \" \\ "` | підтримано `\n \t \" \' \\`; невідома послідовність — символ як є |
| Логічне значення | `істина`, `хиба` | |
| Порожнє значення | `ніщо` | аналог `None` |
| Список | `[1, 2, 3]`, `["а", "б"]`, `[]` | елементи будь-якого типу, можна змішувати |

Немає: багаторядкового («потрійні лапки») рядка, f-рядків/інтерполяції,
словникового літерала `{ключ: значення}`, кортежів, множин.

---

## 3. Змінні

| Ключове слово | Приклад | Значення |
|---|---|---|
| `хай` | `хай вік = 15` | оголосити змінну (перше присвоєння) |
| `стала` | `стала ПІ = 3.14` | оголосити константу — повторне присвоєння дає **помилку компіляції** |
| `=` (просте присвоєння, без `хай`/`стала`) | `вік = вік + 1` | змінити вже оголошену змінну |

Ім'я — будь-який ідентифікатор (літери/цифри/`_`, не з цифри), крім
зарезервованих слів. **`у` — зарезервоване слово** (частина `для x у
список`) і недоступне як ім'я змінної, хоча `уряд`, `умова` тощо — звичайні
ідентифікатори.

Область видимості: усередині `дія` присвоєння створює **локальну** змінну
кадру виклику; читання без попереднього присвоєння в кадрі падає до
глобальних змінних. Тобто дія бачить зовнішні (глобальні) змінні на
читання, але не може змінити їх присвоєнням — воно завжди створює власну
локальну копію (перевірено демо `22_область_видимості.мова`). Замикань і
вкладеного доступу до локальних змінних дії-батька немає.

---

## 4. Оператори

| Категорія | Оператори | Приклад |
|---|---|---|
| Арифметичні | `+ - * / % ` (унарний `-`) | `7 % 2` → `1` |
| Порівняння | `== != < > <= >=` | `5 >= 10` → `хиба` |
| Логічні (з коротким замиканням) | `і`, `або`, `не` | `вік >= 18 і не є_квиток` |
| Присвоєння | `=` | `х = 5` |
| Індексація | `[]` (читання й запис) | `список[0]`, `список[0] = 9` |
| Зріз (лише читання) | `[початок:кінець]`, обидва боки необов'язкові | `список[1:3]`, `список[:2]`, `список[1:]`, `список[:]` |

Немає складених присвоєнь (`+= -= *= /=`), інкременту/декременту
(`++ --`), потрійного оператора, побітових операторів, кроку в зрізі
(`список[::2]`) і присвоєння в зріз (`список[1:3] = ...`).

`+` для рядків — конкатенація (`"а" + "б"` → `"аб"`), для списків —
об'єднання (`[1] + [2]` → `[1, 2]`); якщо хоч один операнд `+` — рядок,
результат завжди рядок (число автоматично перетворюється текстом). Інші
арифметичні оператори (`- * / %`) вимагають, щоб обидва операнди були
числами — інакше помилка виконання.

---

## 5. Вивід і ввід

| Ключове слово | Приклад | Що робить |
|---|---|---|
| `друкуй(...)` | `друкуй("Привіт", 2 + 2)` | виводить аргументи в один рядок через пробіл, з `\n` наприкінці |
| `питай(...)` | `хай імя = питай("Як звати? ")` | виводить (необов'язковий) запит без переносу рядка, чекає ввід з stdin, завжди повертає **рядок** |

---

## 6. Умови

```
якщо вік >= 18
    друкуй("дорослий")
інакше якщо вік >= 13
    друкуй("підліток")
інакше
    друкуй("дитина")
```

`інакше якщо` — не окреме ключове слово, а послідовність `інакше` + вкладене
`якщо` (парситься рекурсивно). Гілки `інакше`/`інакше якщо` необов'язкові.

---

## 7. Цикли

| Ключове слово | Приклад |
|---|---|
| `поки умова` | `поки лічильник > 0\n    лічильник = лічильник - 1` |
| `для імя у ітерований` | `для число у діапазон(1, 4)\n    друкуй(число)` — так само працює зі списком: `для х у [1, 2, 3]` |
| `перервись` | вийти з найближчого циклу негайно |
| `продовж` | перейти до наступної ітерації найближчого циклу |

`перервись`/`продовж` поза циклом — помилка компіляції. `для` реалізовано
компілятором через індекс + `довжина()`, тому працює лише з об'єктами, що
підтримують індексацію й `довжина()` (список, рядок), а не з довільним
«ітерованим» об'єктом.

---

## 8. Списки

| Дія | Приклад |
|---|---|
| Літерал | `хай а = [1, 2, 3]` |
| Індекс (читання), з нуля | `а[0]` |
| Від'ємний індекс (з кінця) | `а[-1]` → останній елемент, `а[-1] = 9` теж працює |
| Індекс (запис) | `а[1] = 99` |
| Зріз (підсписок, лише читання) | `а[1:3]`, `а[:2]`, `а[1:]`, `а[:]`, `а[-2:]` |
| Довжина | `довжина(а)` |
| Об'єднання (як «додавання елемента») | `а = а + [4]` |
| Додати елемент у кінець | `додай(а, 4)` — змінює `а` на місці, повертає `ніщо` |
| Вилучити за індексом | `хай х = вилучи(а, 0)` — змінює `а` на місці, **повертає вилучений елемент**; підтримує від'ємний індекс |
| Сортувати за зростанням | `сортуй(а)` — змінює `а` на місці, повертає `ніщо`; лише однорідний список (усі числа або усі рядки) |
| Перебір | `для х у а\n    друкуй(х)` |

Звичайний індекс (читання й запис) має бути цілим числом; від'ємний
приводиться до `довжина + індекс` (як у Python: `-1` — останній елемент),
і якщо після цього він усе одно поза межами `[0, довжина-1]` — помилка
виконання. **Зріз, навпаки, ніколи не падає через межі**: обидва боки
лагідно обрізаються до `[0, довжина]` (`а[-100:100]` на списку з 3
елементів дає весь список, а не помилку) — так само в Python. Кроку в
зрізі (`а[::2]`) і присвоєння в зріз немає.

Немає інших методів списку (`вставити_за_індексом`, `розвернути`,
`знайти_індекс`, `максимум`/`мінімум`, злиття без `+` тощо) — усе, що не
покрито вище, треба писати вручну.

---

## 9. Рядки

| Дія | Приклад |
|---|---|
| Конкатенація | `"Привіт, " + імя + "!"` |
| Індекс (лише читання), і від'ємний теж | `імя[0]`, `імя[-1]` |
| Зріз (підрядок, лише читання) | `імя[1:3]`, `імя[:2]`, `імя[1:]`, `імя[:]` |
| Довжина | `довжина(імя)` |
| В рядок | `рядок(42)` → `"42"` |
| З рядка в число | `число("42")` → `42`, `число("3.5")` → `3.5` |
| З рядка/числа в ціле | `цілий("3.9")` → `3` |

Рядки незмінні: `імя[0] = "О"` дає помилку виконання (`INDEX_SET`
підтримує лише список і словник). Індекс і зріз працюють так само, як для
списків (розділ 8) — від'ємний індекс з кінця, зріз лагідно обрізається
до меж рядка замість помилки. Немає `верхній()/нижній()`,
`розділи()/зʼєднай()`, пошуку підрядка, форматування рядків.

---

## 10. Дії (функції)

```
дія квадрат_числа(число)
    поверни число * число

хай результат = квадрат_числа(5)
```

| Елемент | Приклад |
|---|---|
| Оголошення з параметрами | `дія імя(п1, п2)` |
| Повернення значення | `поверни вираз` |
| Повернення без значення / кінець тіла без `поверни` | неявно повертає `ніщо` |
| Виклик | `імя(аргумент1, аргумент2)` |
| Рекурсія | працює (демо: факторіал, Фібоначчі); межа глибини викликів — 500 |

Немає: значень параметрів за замовчуванням, змінної кількості аргументів
(`*args`), іменованих аргументів, кількох значень, що повертаються одразу,
анонімних функцій/лямбд, вкладених функцій із доступом до змінних
зовнішньої дії.

**Важлива межа**: ім'я дії не може збігатися з іменем вбудованої функції
(з `yadro/natives.py: ІМЕНА_NATIVE`, наприклад `друкуй`, `квадрат`,
`довжина`, `коло`...) — компілятор явно відмовляє на оголошенні `дія` з
таким іменем («Помилка компіляції», рядок/колонка вказують на `дія`).
Раніше (до фіксу) це була тиха пастка: компілятор завжди компілював виклик
такого імені як `CALL_NATIVE`, і власна однойменна дія просто ніколи не
викликалася (демо `21_тінь_вбудованої_функції.мова` тепер показує саме
помилку компіляції, а не мовчазний no-op).

---

## 11. Підключення інших файлів (`підключи`)

```
підключи "фігури.мова"

друкуй(площа_квадрата(5))
```

| Елемент | Приклад | Поведінка |
|---|---|---|
| `підключи "шлях.мова"` | `підключи "утиліти.мова"`, `підключи "тека/утиліти.мова"` | зливає дії (і сталі/змінні верхнього рівня) підключеного файлу в поточний, ніби їх написано тут |

Шлях у лапках рахується **відносно директорії файлу, що підключає**, а не
поточної робочої директорії — тож `головний.мова` і `утиліти.мова` в одній
теці підключаються просто як `"утиліти.мова"` незалежно від того, звідки
запущено `мова`. `підключи` дозволено лише **на верхньому рівні файлу** —
всередині `дія`/`якщо`/`поки`/`для` це помилка розбору.

Опрацьовані помилкові випадки (усі — окремий клас `ПомилкаПідключення`, з
рядком/позицією/стрілкою `^` під самою інструкцією `підключи`, що
спричинила помилку):

| Випадок | Приклад | Повідомлення |
|---|---|---|
| Файл не знайдено | `підключи "немає.мова"` | «Файл «немає.мова» не знайдено (шукав: ...).» |
| Циклічне підключення | `а.мова` підключає `б.мова`, а той — знову `а.мова` (включно з файлом, що підключає сам себе) | «Циклічне підключення файлів: а.мова -> б.мова -> а.мова.» |
| Конфлікт імен | і в головному файлі, і в підключеному є `дія двічі(...)` | «Конфлікт імен: дія «двічі» вже визначена у файлі «...», а тепер визначається ще й у «...».» |

Той самий файл, підключений з кількох місць одразу («діамантове»
підключення — наприклад, і `а.мова`, і `б.мова` підключають спільний
`база.мова`), підключається **лише один раз** — це не помилка, і код
`база.мова` не виконується вдруге.

Реалізовано окремим кроком **до** компіляції (`yadro/moduli.py`), а не
новим опкодом: `підключи` рекурсивно читає й розпарсює файли, підставляючи
їх інструкції на місце `підключи`, і лише тоді віддає одну плоску
програму компілятору — компілятор і VM про файли/модулі нічого не знають.
Тому підтримується лише при запуску/компіляції файлу (`мова файл.мова`,
`мова -к`, `мова -д`) — **не в REPL**, де рядки компілюються без доступу
до файлової системи.

Немає: підключення за іменем пакета/бібліотеки (лише за шляхом до
файлу), вибіркового підключення окремих імен (`з X підключи Y`),
псевдонімів (`підключи X як Y`), відносних шляхів вище кореня (`../`
працює технічно як звичайний шлях, але не перевірявся окремо).

---

## 12. Вбудовані («мовні») функції

Реалізовані прямо в `yadro/vm.py`, доступні завжди:

| Функція | Приклад | Що робить |
|---|---|---|
| `друкуй(...)` | `друкуй("х =", х)` | вивід |
| `питай(запит)` | `питай("Ім'я? ")` | ввід рядка |
| `спи(секунди)` | `спи(0.5)` | призупинити виконання |
| `довжина(х)` | `довжина([1,2,3])` → `3` | довжина списку/рядка/словника |
| `рядок(х)` | `рядок(42)` → `"42"` | будь-яке значення в текст |
| `число(х)` | `число("3.5")` → `3.5` | рядок/число в число |
| `цілий(х)` | `цілий("3.9")` → `3` | у ціле число |
| `діапазон(a[, b[, c]])` | `діапазон(1, 10, 2)` | список цілих чисел (як `range`) |
| `додай(список, х)` | `додай(а, 4)` | додає `х` у кінець `список` (на місці), повертає `ніщо` |
| `вилучи(список, і)` | `вилучи(а, 0)` → вилучений елемент | вилучає елемент за індексом (і від'ємним теж) з `список` (на місці), **повертає його** |
| `сортуй(список)` | `сортуй(а)` | сортує `список` за зростанням на місці, повертає `ніщо`; лише однорідний список (усі числа або усі рядки) |
| `словник(...)` | `словник("а", 1, "б", 2)` | новий словник (0.5, розділ 14); `словник()` — порожній |
| `ключі(словник)` | `ключі(д)` → `[а, б]` | список ключів у порядку додавання |
| `кинь(...)` | `кинь("завелике", n)` | зупинити з власною помилкою; текст ловиться блоком `спробуй` (розділ 17) |
| `прочитай_файл(шлях)` | `прочитай_файл("нотатки.txt")` | вміст файлу як рядок (розділ 15) |
| `запиши_файл(шлях, текст)` | `запиши_файл("н.txt", "привіт")` | створити/перезаписати файл |
| `допиши_файл(шлях, текст)` | `допиши_файл("лог.txt", рядок + "\n")` | дописати в кінець (створює, якщо немає) |
| `є_файл(шлях)` | `якщо є_файл("н.txt")` | `істина`/`хиба` |

---

## 13. Розширені вбудовані функції (виходять за межі «базового мінімуму»)

Ці категорії не є частиною ядра мови, а є місточками до Android/Termux,
локальної LLM і графіки. Для запуску без реального Termux/моделі є прапор
`--без-пристрою` (усі виклики лише друкують, що вони зробили б, і
повертають заглушки):

- **Пристрій** (`yadro/prystriy.py`): `вібруй`, `сповісти`, `скажи`, `тост`,
  `яскравість`, `гучність`, `батарея`, `ліхтар`, `буфер`, `запиши_буфер`,
  `датчик`, `зніми`, `місце`.
- **Локальна LLM** (`yadro/rozum.py`): `спитай`, `переклади`, `стисни`.
- **Малювання у SVG** (`yadro/drawing.py`): `полотно`, `квадрат`, `коло`,
  `лінія`, `текст`, `покажи`.
- **Малювання в терміналі** (`yadro/ekran.py`): `екран`, `точка`,
  `запов_квадрат`, `запов_коло`, `відрізок`, `намалюй`, `тримай`.
- **Креслення / ЄСКД** (`yadro/drafting.py`): `аркуш`, `впиши_в_аркуш`,
  `вид`, `ізометрія`, `контур`, `лінія3д`, `осьова`, `невидима`, `розмір`,
  `розмір_радіус`, `розмір_діаметр`, `виміряй`, `виміряй_кут`, `кут`,
  `виноска`, `штамп`, `покажи`.
- **Параметричний ескіз** (`yadro/sketch.py`): `вузол`, `сегмент`,
  `габарити`, `зсунь`, `вертикально`, `горизонтально`, `рівні`, `довжина`,
  `кут_між`, `збіг`, `на_лінії`, `розвяжи`.

(`покажи` і `довжина` — спільні імена для двох різних реалізацій;
диспетчеризуються у `вм.py` за станом/кількістю аргументів — див.
розділ «Відомі каверзні моменти» в `README.md`.)

---

## 14. Словники (з 0.5)

| Дія | Приклад |
|---|---|
| Порожній словник | `хай д = словник()` |
| З парами | `хай д = словник("яблуко", 3, "груша", 5)` |
| Літерал | `хай д = {"яблуко": 3, "груша": 5}` (`{}` — порожній) |
| Запис за ключем | `д["слива"] = 1` |
| Запис без лапок (ключ — слово) | `д.слива = 1` |
| Читання | `д["яблуко"]`, `д.яблуко` — відсутній ключ → помилка виконання |
| Чи є ключ | `якщо "яблуко" у д`, `якщо "манго" не у д` |
| Перебір ключів | `для к у д\n    друкуй(к, д[к])` — у порядку додавання |
| Список ключів | `ключі(д)` |
| Вилучити ключ | `хай з = вилучи(д, "яблуко")` — повертає значення |
| Кількість | `довжина(д)` |

Ключ — рядок, число або логічне значення (список/словник/екземпляр типу
ключем бути не може). `д.ім'я` — те саме, що `д["ім'я"]`, тільки без
лапок: працює лише коли ключ виглядає як ім'я змінної. Оператор `у`
працює також зі списком (`2 у [1, 2]`) і рядком (`"ри" у "привіт"`).
Словники, які повертають `батарея()`/`датчик()`/`місце()`, — звичайні
словники: до них це все теж застосовно.

---

## 15. Файли (з 0.5)

| Дія | Приклад |
|---|---|
| Прочитати у рядок | `хай т = прочитай_файл("нотатки.txt")` |
| Записати (перезаписати) | `запиши_файл("нотатки.txt", "перший рядок\n")` |
| Дописати в кінець | `допиши_файл("нотатки.txt", "ще рядок\n")` |
| Чи існує | `якщо є_файл("нотатки.txt")` |

Відносний шлях рахується **від теки застосунку**, а не від поточної
робочої теки: для `мова.py програма.мова` — це тека самої програми, у
Android-застосунку — приватна тека застосунку (`user_data_dir`, єдине
місце, куди APK гарантовано може писати), у REPL — поточна тека.
Абсолютні шляхи — як є. Підтеки (`"дані/в.txt"`) створюються самі. Усі
збої (немає файлу, немає прав, не UTF-8) — звичайна помилка виконання,
тож її можна перехопити `спробуй`. Не-рядок у `запиши_файл` пишеться як
текст (`42` → `"42"`).

---

## 16. Типи — структури з полями й діями (з 0.5)

```
тип Точка
    x
    y = 0                      # типове значення; без нього поле = ніщо
    дія зсунь(dx, dy)          # дія типу; «я» — сам екземпляр, у дужках не пишеться
        я.x = я.x + dx
        я.y = я.y + dy
    дія опис()
        поверни "(" + я.x + ", " + я.y + ")"

тип Лінія
    початок
    кінець

хай т = Точка(1, 2)            # значення — у порядку полів; хвіст можна пропустити
т.x = 10                       # зміна поля
т.зсунь(1, 1)                  # виклик дії
друкуй(т.опис(), т)            # (11, 3) Точка(x: 11, y: 3)
хай л = Лінія(Точка(0, 0), Точка(5, 5))
л.кінець.x = 7                 # вкладені поля
```

| Дія | Приклад |
|---|---|
| Оголосити тип | `тип Точка` + блок: рядки-поля (`x` або `x = 0`) і `дія ...` |
| Створити екземпляр | `Точка(1, 2)`, `Точка(1)` (решта — типові), `Точка()` |
| Поле | `т.x`, `т.x = 5` |
| Дія типу | `т.зсунь(1, 2)`; всередині дії екземпляр — це `я` |
| Друк | `друкуй(т)` → `Точка(x: 1, y: 2)` |
| Порівняння | `Точка(1, 2) == Точка(1, 2)` → `істина` (за полями) |

Екземпляр, як і список, змінюється на місці: `хай б = а` — та сама точка.
Типове значення-список/словник у кожного екземпляра своє. Нових полів
після оголошення додавати не можна (помилка виконання «немає поля»);
`я` — не ключове слово, а неявний перший параметр дії типу (вказувати
його в дужках — помилка розбору). Тип не можна назвати іменем
вбудованої функції (`тип коло` — помилка компіляції). Спадкування немає.

---

## 17. Обробка помилок: `спробуй` / `якщо помилка` (з 0.5)

```
спробуй
    хай х = число(питай("Число? "))
    друкуй(100 / х)
якщо помилка
    друкуй("Не вийшло:", помилка)     # помилка — рядок з текстом помилки
друкуй("Програма живе далі")
```

| Дія | Приклад |
|---|---|
| Перехопити помилку | `спробуй` + блок, потім `якщо помилка` + блок |
| Текст помилки | змінна `помилка` (звичайна змінна, доступна й після блоку) |
| Проковтнути тихо | `спробуй` + блок без `якщо помилка` |
| Кинути свою помилку | `кинь("від'ємна довжина", n)` |
| Вкладення | `спробуй` всередині `спробуй`/дії/циклу; помилка в обробнику йде далі назовні |

Ловляться **помилки виконання** (ділення на нуль, невідома змінна,
індекс поза межами, порівняння різних типів, помилки файлів,
`кинь(...)`, помилки з дій будь-якої глибини). Помилки розбору й
компіляції — ні (їх видно ще до запуску), і кнопка «Стоп» теж не
перехоплюється. `перервись`/`продовж`/`поверни` зсередини `спробуй`
працюють як звичайно. «`якщо помилка`» впізнається лише одразу після
блоку `спробуй` і лише як рядок із цих двох слів; `помилка` при цьому
лишається звичайним ім'ям (`якщо помилка == "x"` — звичайна умова).

---

## Додаток: опкоди байткоду VM

Стандартні за ТЗ: `PUSH_CONST PUSH_VAR STORE_VAR POP ADD SUB MUL DIV MOD
NEG EQ NEQ LT GT LTE GTE AND OR NOT JUMP JUMP_FALSE CALL RETURN
CALL_NATIVE HALT`.

Додано понад ТЗ (без них не працюють дії й списки): `MAKE_FUNCTION
MAKE_LIST INDEX_GET INDEX_SET SLICE_GET`.

Додано у 0.5: `MAKE_DICT n` (словник із 2n значень на стеку),
`CONTAINS прапорець_заперечення` («у»/«не у»), `GET_FIELD ім'я` /
`SET_FIELD ім'я` (поле екземпляра або ключ словника), `MAKE_TYPE [ім'я,
[поля]]` (типові значення полів — зі стека; дії типу — звичайні
`MAKE_FUNCTION` з іменем `Тип.дія` і першим параметром `я`),
`CALL_METHOD [ім'я, кількість_аргументів]`, `TRY_BEGIN адреса_обробника`
/ `TRY_END`. Конструктор `Точка(1, 2)` — звичайний `CALL`: VM дивиться
спершу в дії, потім у типи. Перебір словника в `для` — через службову
вбудовану `@перелік` (словник → список ключів).

`підключи` (розділ 11) — виняток: **не** новий опкод. Файли зливаються в
одну програму до компіляції (`yadro/moduli.py`), тож компілятор і VM
бачать звичайний код без жодного сліду підключення.

Подивитись згенерований байткод для будь-якого файлу:

```
мова -д файл.мова
```
