# Підсумок: методи списків, від'ємні індекси, зрізи

**Стан:** уже реалізовано й закомічено раніше (коміт `2e0ef9e`, "Додати
методи списків (додай/вилучи/сортуй), від'ємні індекси й зрізи").
У цій сесії запит співпав із вже виконаною роботою — новий код не
знадобився, зроблено лише повторну перевірку.

## Що перевірено

- **Тести**: `python3 -m pytest тести/` (без `test_живий.py`, `test_екран.py`
  — керують реальним терміналом, і `test_репл.py`/`test_пристрій.py` — які
  в цьому середовищі зависають на реальному stdin/термукс-вібрації,
  не пов'язано зі списками) → **176 passed**.
- **Ручний прогін** (`мова.py --без-пристрою`) на невеликому скрипті:

  ```
  хай а = [1, 2, 3, 4, 5]
  друкуй(а[-1])        → 5
  друкуй(а[1:3])       → [2, 3]
  додай(а, 6)
  друкуй(а)            → [1, 2, 3, 4, 5, 6]
  хай х = вилучи(а, 0)
  друкуй(х)            → 1
  друкуй(а)            → [2, 3, 4, 5, 6]
  сортуй(а)
  друкуй(а)            → [2, 3, 4, 5, 6]
  ```

- **ДОВІДНИК.md**: розділи 8 (списки), 9 (рядки), 11 (вбудовані функції) і
  додаток з опкодами вже містять `додай`/`вилучи`/`сортуй`, від'ємні
  індекси й зрізи; застарілих фраз на кшталт «немає від'ємних індексів»
  не лишилось (перевірено `grep`).

## Що саме є в мові (без змін відносно попереднього звіту)

| Можливість | Приклад | Поведінка |
|---|---|---|
| `додай(список, х)` | `додай(а, 4)` | дописує в кінець на місці, повертає `ніщо` |
| `вилучи(список, і)` | `вилучи(а, 0)` | вилучає за індексом (і від'ємним), повертає вилучений елемент |
| `сортуй(список)` | `сортуй(а)` | сортує за зростанням на місці; лише однорідний список |
| Від'ємний індекс | `а[-1]`, `а[-1] = 9` | рахується з кінця; поза межами — помилка виконання |
| Зріз | `а[1:3]`, `а[:2]`, `а[1:]`, `а[:]` | лагідно обрізається до меж, без кроку й без присвоєння в зріз |

Детальний перелік змінених файлів і повний список тестів — у
`ЗВІТ_СПИСКИ.md` (звіт попередньої сесії, не змінювався).

## Відомі обмеження середовища (не пов'язані з задачею)

`тести/test_пристрій.py` зависає в цьому контейнері (немає термукс-оточення
для `termux-vibrate`) — існувало до цієї сесії, до списків стосунку не має.
