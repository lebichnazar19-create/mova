# Підручник з мови «Мова»

Цей підручник — для тих, хто ніколи не програмував. Кожна тема має короткий
приклад коду. Скопіюй його у файл з розширенням `.мова` (наприклад,
`урок.мова`) і запусти командою:

```
мова урок.мова
```

Після кожного прикладу написано, що саме з'явиться на екрані.

---

## 1. `друкуй` — показати щось на екрані

```
друкуй("Привіт!")
друкуй(2 + 2)
```

**Що станеться:** на екрані з'явиться два рядки:

```
Привіт!
4
```

`друкуй` — це слово, яке каже комп'ютеру: "покажи це на екрані". Текст
пишеться в лапках (`"Привіт!"`), а число — просто так (`2 + 2`, комп'ютер
сам порахує й покаже `4`). Можна показати кілька речей одразу через кому:

```
друкуй("Результат:", 2 + 2)
```

Покаже: `Результат: 4`

---

## 2. Змінні — як запам'ятати значення

Щоб не писати те саме число чи текст кілька разів, йому можна дати ім'я.
Для цього є слово `хай`:

```
хай вік = 15
друкуй("Тобі буде через рік:", вік + 1)
```

**Що станеться:** з'явиться `Тобі буде через рік: 16`

`вік` тут — це змінна: коробочка з іменем, у якій лежить значення `15`.
Значення можна міняти:

```
хай гроші = 100
друкуй(гроші)
гроші = гроші - 30
друкуй(гроші)
```

Покаже спочатку `100`, потім `70`. Зверни увагу: коли ми *створюємо*
змінну вперше, пишемо `хай`. Коли просто *міняємо* вже створену — `хай`
більше не пишемо, тільки `гроші = ...`.

Якщо значення взагалі ніколи не повинно змінюватись (наприклад, кількість
днів у тижні), замість `хай` можна написати `стала` — тоді комп'ютер сам
не дасть його випадково змінити далі в програмі.

---

## 3. `питай` — запитати щось у людини

```
хай імя = питай("Як тебе звати? ")
друкуй("Привіт, " + імя + "!")
```

**Що станеться:** програма покаже `Як тебе звати? ` і зупиниться, чекаючи,
поки ти щось набереш і натиснеш Enter. Потім скаже, наприклад:
`Привіт, Оля!`

`питай` завжди повертає те, що людина ввела, як текст. Знак `+` між
текстами склеює їх в один рядок (тому і `+ "!"` в кінці).

---

## 4. `якщо` — умова: зроби щось, тільки якщо це правда

```
хай вік = 15

якщо вік >= 18
    друкуй("Ти дорослий.")
інакше
    друкуй("Ти ще дитина.")
```

**Що станеться:** покаже `Ти ще дитина.` (бо 15 не більше й не дорівнює 18).

Дуже важливе правило мови: рядки, які належать до `якщо` (або до будь-якої
іншої умови чи циклу нижче), пишуться з **відступом у 4 пробіли**. Це не
краса заради краси — саме відступ показує комп'ютеру, які рядки виконати,
якщо умова правдива, а які — ні. Немає відступу — комп'ютер не зрозуміє,
де закінчується "якщо" і починається щось інше.

Знаки для порівняння: `==` (дорівнює), `!=` (не дорівнює), `<`, `>`, `<=`,
`>=` (менше, більше, менше-або-дорівнює, більше-або-дорівнює). Зверни
увагу: `==` (два знаки) — це порівняння, а `=` (один знак) — це
присвоєння значення змінній, як у розділі 2.

Якщо варіантів більше двох, є `інакше якщо`:

```
хай оцінка = 7

якщо оцінка >= 10
    друкуй("Відмінно")
інакше якщо оцінка >= 7
    друкуй("Добре")
інакше
    друкуй("Треба підтягнути")
```

Покаже `Добре`.

Умови можна комбінувати словами `і` (обидві частини мають бути правдою),
`або` (досить хоча б однієї) і `не` (перевертає правду на неправду):

```
хай вік = 20
хай є_квиток = хиба

якщо вік >= 18 і не є_квиток
    друкуй("Потрібно купити квиток")
```

`істина` і `хиба` — це два спеціальні значення мови: "так, це правда" і
"ні, це неправда".

---

## 5. `поки` — повторюй, поки умова правдива

```
хай лічильник = 3

поки лічильник > 0
    друкуй(лічильник)
    лічильник = лічильник - 1

друкуй("Старт!")
```

**Що станеться:**

```
3
2
1
Старт!
```

`поки` перевіряє умову, і якщо вона правдива — виконує рядки з відступом,
а тоді перевіряє умову знову, і знову, і знову... Тому всередині `поки`
майже завжди потрібен рядок, який рано чи пізно зробить умову неправдивою
(тут — `лічильник = лічильник - 1`). Якщо забути про це, програма
зациклиться назавжди, і її доведеться зупиняти вручну.

---

## 6. `для` — повторити для кожного елемента

Коли наперед відомо, скільки разів повторити, чи є готовий перелік речей,
зручніше `для`:

```
для число у діапазон(1, 4)
    друкуй(число)
```

**Що станеться:**

```
1
2
3
```

`діапазон(1, 4)` — це "усі цілі числа від 1 до 4, не рахуючи саму 4".
`для число у ...` означає: "для кожного значення з цього переліку, по
черзі назви його `число` і виконай рядки з відступом".

---

## 7. Списки — кілька значень в одній коробочці

Список — це впорядкований перелік значень в квадратних дужках:

```
хай фрукти = ["яблуко", "банан", "груша"]
друкуй(фрукти)
друкуй(фрукти[0])
друкуй(довжина(фрукти))
```

**Що станеться:**

```
[яблуко, банан, груша]
яблуко
3
```

Кожен елемент списку має номер — **індекс**, і рахунок починається з
**нуля**, а не з одиниці. Тому `фрукти[0]` — це перший елемент
(`"яблуко"`), а `фрукти[1]` — другий (`"банан"`). `довжина(...)` показує,
скільки всього елементів.

Списки чудово поєднуються з `для`:

```
хай друзі = ["Оля", "Іван", "Марко"]

для імя у друзі
    друкуй("Привіт, " + імя + "!")
```

Покаже привітання для кожного імені зі списку, по одному на рядок.

Змінити елемент можна через його індекс:

```
хай кошик = [10, 20, 30]
кошик[1] = 99
друкуй(кошик)
```

Покаже `[10, 99, 30]`.

Готової команди "додати елемент" у мові немає, але це легко зробити
самому — скласти старий список з новим елементом (у дужках!):

```
хай кошик = [10, 20]
кошик = кошик + [30]
друкуй(кошик)
```

Покаже `[10, 20, 30]`.

---

## 8. Дії — шматок коду, який можна викликати багато разів

Коли якийсь код повторюється (наприклад, привітання), його зручно
оформити як **дію** — назвати один раз і потім просто "кликати" за
іменем:

```
дія привітай(імя)
    друкуй("Привіт, " + імя + "!")

привітай("Оля")
привітай("Іван")
```

**Що станеться:**

```
Привіт, Оля!
Привіт, Іван!
```

`імя` тут — **параметр**: значення, яке дія отримує ззовні кожного разу,
коли її викликають. Дія може не тільки щось друкувати, а й **повертати**
результат назад — для цього є слово `поверни`:

```
дія квадрат_числа(число)
    поверни число * число

хай результат = квадрат_числа(5)
друкуй(результат)
```

Покаже `25`. Рядок `поверни число * число` рахує значення і віддає його
туди, звідки дію викликали — тому його можна одразу покласти в змінну
(`хай результат = ...`).

---

## 9. Пристрій — керуємо телефоном прямо з коду

Мова вміє напряму керувати телефоном: вібрація, сповіщення, ліхтарик,
заряд батареї тощо. Найпростіший приклад:

```
вібруй(300)
```

**Що станеться:** телефон завібрує на 300 мілісекунд (0.3 секунди).

```
сповісти("Привіт", "Це сповіщення з мого коду!")
```

**Що станеться:** у шторці сповіщень з'явиться повідомлення.

Найцікавіше — коли пристрій поєднується з `якщо`. `батарея()` повертає
не просто число, а невеличку "коробочку з підписаними відділеннями" —
у ній під підписом `"percentage"` лежить саме відсоток заряду:

```
хай інфо = батарея()
хай заряд = інфо["percentage"]

друкуй("Заряд:", заряд, "%")

якщо заряд < 20
    сповісти("Батарея", "Заряд низький!")
    вібруй(500)
інакше
    друкуй("Все гаразд.")
```

**Що станеться:** покаже поточний заряд, і якщо він менший за 20% —
надішле сповіщення й завібрує; інакше просто скаже, що все гаразд.

---

## 10. Усе разом — показова програма `приклади/показ.мова`

Коли уроки 1–9 пройдено, відкрий `приклади/показ.мова`: це одна справжня
програма — облік робочих годин електрика за тиждень. Вона вводить дні й
години, рахує суму й надбавку за понаднормові, зберігає звіт у файл,
читає його назад і показує. Запусти її:

```
мова --без-пристрою приклади/показ.мова
```

**Що станеться:** програма надрукує, що п'ятницю з від'ємними годинами
пропущено, покаже звіт із файлу (усього 41.5 год, 6 понаднормових,
11125 грн до виплати), підсумок, а потім завібрує, надішле сповіщення,
покаже тост і озвучить суму годин (без телефона — просто напише, що б
зробила).

Кожен блок у файлі підписано одним рядком-коментарем. Крім знайомого з
уроків, там є чотири нові речі (докладно — у `ДОВІДНИК.md`, розділи
14–17):

- **словник** — коробочка з підписаними відділеннями, яку ти вже бачив у
  `батарея()`, тільки тепер її можна зробити самому:
  `хай ввід = словник()`, `ввід["Понеділок"] = 8`, `ввід.Субота = 4`,
  `якщо "П'ятниця" у пропущені`, `для назва у ввід`;
- **тип** — свій вид значення з полями і діями: `тип День` з полями
  `назва`, `години` і дією `понаднормові()`, всередині якої `я` — це
  сам день; створюється як `День("Вівторок", 10)`, читається `день.години`;
- **спробуй / якщо помилка** — щоб програма не падала: помилка з
  `кинь(...)` або з `прочитай_файл` потрапляє у змінну `помилка`, і
  програма йде далі;
- **файли** — `запиши_файл`, `допиши_файл`, `є_файл`, `прочитай_файл`;
  файл з'являється поруч із програмою (у застосунку — у його теці).

Спробуй змінити години в словнику `ввід`, ставку `СТАВКА` або додати
неділю — і запустити знову.

---

## 11. Математика, рядки й час — готові помічники

У мові є набір вбудованих дій, які не треба писати самому. Приклад:

```
друкуй("корінь з 16 =", корінь(16))
друкуй("2 у 10 степені =", степінь(2, 10))
друкуй("округлимо:", округли(3.14159, 2), округли(2.5))
друкуй("найменше й найбільше:", мінімум(7, 3, 9), максимум([7, 3, 9]))
друкуй("площа кола з радіусом 2:", округли(ПІ * 2 * 2, 2))
друкуй("кубик показав", випадкове(1, 6))
```

**Що станеться:** `4`, `1024`, `3.14 3`, `3 9`, `12.57` і випадкове
число від 1 до 6 — щоразу інше. `ПІ` — стала, вона є завжди.

З рядками теж є помічники:

```
хай речення = "  мова це просто  "
хай слова = розбий(обріж(речення))
друкуй(слова, довжина(слова))
друкуй(з'єднай(слова, "-"))
друкуй(великими("тихо"), заміни("кіт і кіт", "кіт", "пес"))
якщо містить(речення, "просто") і починається(обріж(речення), "мова")
    друкуй("так, це про мову")
```

**Що станеться:** `[мова, це, просто] 3`, `мова-це-просто`,
`ТИХО пес і пес`, `так, це про мову`. Зверни увагу на апостроф у
`з'єднай` — він частина назви, так само можна назвати змінну `ім'я`.

Перетворення між текстом і числом: `число("42")` дає число `42`,
`текст(42)` дає рядок `"42"`, `дробове("2,5")` дає `2.5`.

І час:

```
друкуй("зараз", зараз(), "— сьогодні", сьогодні())
якщо година() < 12
    друкуй("Доброго ранку!")
хай старт = секунди()
спи(1)
друкуй("минуло", округли(секунди() - старт, 1), "с")
```

**Що станеться:** дата з часом, привітання (якщо ранок) і `минуло 1.0 с`.
`секунди()` рахує від довільної точки, тому вони корисні лише різницею —
для вимірювання, скільки щось тривало.

Файли з уроку 10 доповнились трьома діями: `рядки_файлу("н.txt")` дає
список рядків, `файли_в_теці()` — імена файлів поруч із програмою,
`видали_файл("н.txt")` — видаляє. Усе разом — у `приклади/математика.мова`.

А `питай("Скільки годин? ")` з уроку 3 тепер працює і в застосунку на
телефоні: під виводом з'являється запит із полем і кнопкою «Надіслати».

---

## 12. Точки на площині — перший крок до креслення

`Точка(x, y)` — це пара координат, з якою можна рахувати як з числом.
Точки живуть у бібліотеці «геометрія», тож спершу її треба підключити —
один рядок на початку програми (без нього мова підкаже, що саме дописати):

```
підключи("геометрія")

хай а = Точка(3, 4)
хай б = Точка(1, 1)
друкуй(а, а + б, а * 2, а.x, а.y)
друкуй("від початку координат:", довжина(а), " між точками:", відстань(а, б))
друкуй("посередині:", середина(а, б), " кут напрямку:", кут(б, а))
```

**Що станеться:** `(3, 4) (4, 5) (6, 8) 3 4`, потім `5` і `3.605551275`,
потім `(2, 2.5)` і кут `45` (градусів від осі x, проти годинникової).

Точку не можна змінити — натомість є дії, що повертають нову:

```
хай кути = [Точка(0, 0), Точка(4, 0), Точка(4, 3), Точка(0, 3)]
друкуй(зсунь(кути, 1, 1))
друкуй(оберни(Точка(1, 0), 90))
друкуй(масштабуй(кути, 2, Точка(2, 1.5)))
```

**Що станеться:** ті самі чотири кути, зсунуті на 1 вправо і вгору;
`(0, 1)` — точка, обернута на 90°; прямокутник удвічі більший навколо
свого центру. Можна передати одну точку або одразу список.

Прямі й перетини — для планів і креслень:

```
хай перетин = перетин_прямих(Точка(0, 0), Точка(2, 2), Точка(0, 2), Точка(2, 0))
друкуй("перетин діагоналей:", перетин)
друкуй("від лампи до стіни:", відстань_до_прямої(Точка(2, 1.5), Точка(0, 0), Точка(4, 0)))
друкуй("де коло зачепить стіну:", перетин_кола_з_прямою(Точка(2, 1.5), 1.5, Точка(0, 0), Точка(4, 0)))
```

**Що станеться:** `(1, 1)`, `1.5` і список з однієї точки `[(2, 0)]` —
коло радіусом 1.5 з центром на висоті 1.5 якраз торкається підлоги. Якщо
прямі паралельні, `перетин_прямих` дає `ніщо`.

Повний приклад — `приклади/геометрія.мова`: кімната 4×3 м, розетки на
стінах, і скільки кабелю треба від щитка до кожної, якщо вести його по
стінах.

---

## 13. Кнопки — програма, що чекає на тебе

У застосунку на телефоні програма може показати власні кнопки:

```
хай лічильник = 0

дія плюс()
    лічильник = лічильник + 1
    друкуй("Лічильник:", лічильник)

дія скинути()
    лічильник = 0
    друкуй("Скинуто.")

кнопка("+1", плюс)
кнопка("Скинути", скинути)
```

**Що станеться:** під виводом з'являться дві кнопки. Програма не
завершиться, а чекатиме: кожне натискання «+1» виконає дію `плюс` і
надрукує новий лічильник. Зупинити — «Стоп» або «Очистити вивід». У
консолі кнопки лише друкуються як `[кнопка +1]`, і програма одразу
закінчується.

Другий аргумент `кнопка` — назва дії без дужок: не `плюс()`, а `плюс`.
Дія має бути без параметрів. Усередині дії можна питати (`питай`),
друкувати, працювати з файлами — усе як завжди. Повний приклад —
`приклади/кнопки.мова` (список імен, «Показати» і «Додати»).

Дві дрібниці про застосунок: кнопка «Довідка» друкує у вивід усі слова
й дії мови з прикладами (коли забув, як щось зветься), а помилки розбору
тепер підказують правильну форму — напиши `словник а`, і мова відповість,
що словник викликають так: `хай д = словник()`.

---

## 14. Креслення на телефоні — маленький CAD

Усе з попередніх уроків збирається в одне: ескіз із прив'язками,
точки, креслення й полотно в застосунку.

```
вузол("а", 0, 0)
вузол("б", 50, 3)
вузол("в", 48, 30)
вузол("г", 2, 28)
сегмент("низ", "а", "б")
сегмент("права", "б", "в")
сегмент("верх", "в", "г")
сегмент("ліва", "г", "а")

горизонтально("низ")
під_кутом("права", "низ", 90)
паралельно_до("верх", "низ")
паралельно_до("ліва", "права")
довжина("низ", 50)
довжина("права", 30)

вузол("розетка", 10, 0)
на_стіні("розетка", "низ", 12)
розвяжи()

аркуш(150, 100, 3)
впиши_в_аркуш(10)
контур([вузол("а"), вузол("б"), вузол("в"), вузол("г")])
розмір(вузол("а"), вузол("б"), -10)
розмір(вузол("б"), вузол("в"), 10)
виноска(вузол("розетка"), "розетка")
штамп("Кімната", "план", "1:1")
```

**Що станеться:** розв'язувач підправить грубі координати так, щоб
вийшов прямокутник 50×30 з розеткою на нижній стіні за 12 мм від кута.
Додай у кінці `покажи()` — на комп'ютері відкриється `креслення.html` з
аркушем: рамка, штамп, контур, два розміри й виноска. На телефоні аркуш
на полотні показує застосунок, зроблений із програми («Зробити
застосунок»): гортай пальцем, масштабуй двома пальцями, «Вмістити все»
повертає аркуш повністю.

Прив'язки словами: `на_стіні(що, стіна, від_кута)`, `на_відстані(що,
від_чого, скільки)`, `паралельно_до(що, чого)`, `під_кутом(що, чого,
градуси)`. `вузол("а")` після `розвяжи()` — це `Точка`, тож її можна
одразу класти в `контур`, `розмір`, `відстань`, `середина` (для двох
останніх потрібен `підключи("геометрія")` — урок 12).

Свою програму збережи кнопкою «Зберегти» (спитає назву), а потім
знайдеш її через «Відкрити». Повний приклад —
`приклади/кімната_креслення.мова`.

---

## 15. Екран — малюємо на полотні

Бібліотека «екран» дає полотно в пікселях: `(0, 0)` — лівий верхній кут,
x росте праворуч, y — вниз. Спершу обираєш колір, потім малюєш, а
наприкінці кажеш `онови()` — тільки тоді намальоване з'являється.

```
підключи("екран")

друкуй("Полотно:", ширина(), "на", висота())
колір(30, 136, 229)
заливка(20, 20, 120, 80)
колір("червоний")
круг(200, 60, 40)
колір(0, 0, 0)
лінія(0, висота(), ширина(), 0)
онови()
```

**Що станеться:** у застосунку, зробленому з цієї програми («Зробити
застосунок», урок 14), полотно покаже синій прямокутник, червоний круг
і чорну лінію з лівого нижнього кута в правий верхній (кнопка
«Креслення» перемикає полотно з виводом). На комп'ютері й у редакторі
програма лише надрукує розмір — `Полотно: 360 на 480` — бо полотна там
немає: малювання йде «в симуляцію», як плата без кабелю, і нічого не
падає.

Колір — три числа від 0 до 255 (червоний, зелений, синій), назва
(«синій», «червоний», «зелений», «жовтий», «чорний», «білий», «сірий»)
або запис `"#RRGGBB"`. Фігури: `крапка(x, y)`, `лінія(x1, y1, x2, y2)`,
`прямокутник(x, y, ширина, висота)` і `заливка(…)` — те саме, але
залите, `коло(x, y, радіус)` і `круг(…)` — залите коло. `очисти()`
прибирає все.

Рух — це цикл: прибрати, намалювати трохи далі, показати, зачекати:

```
підключи("екран")

хай x = 0
поки x < ширина()
    очисти()
    колір("зелений")
    круг(x, висота() / 2, 30)
    онови()
    спи(0.05)
    x = x + 10
```

**Що станеться:** зелений круг проїде зліва направо через середину
полотна. Повний приклад із сіткою — `приклади/екран.мова`.

---

## Що далі

У папці `приклади/навчання/` є десять маленьких готових програм — від
найпростішої до такої, що поєднує все з цього підручника разом. Відкрий
їх по черзі, читай коментарі всередині (рядки, що починаються з `#`) і
пробуй міняти числа й текст та запускати знову — це найкращий спосіб
зрозуміти, як усе працює. Коли захочеться побачити все одразу в одній
програмі — `приклади/показ.мова` (урок 10).
